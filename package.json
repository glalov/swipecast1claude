{
  "name": "swipecast",
  "version": "0.1.0",
  "private": true,
  "description": "SwipeCast — casting platform where every headshot gets seen.",
  "scripts": {
    "build": "python3 build-html.py",
    "dev": "python3 serve.py"
  }
}
